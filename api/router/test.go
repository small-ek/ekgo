package router

//后台管理
/*func Test(Router *gin.RouterGroup, endpoints endpoint.Endpoint) {

	Group := Router.Group("/test")
	{
		Group.GET("/status", func(ctx *gin.Context) {
			http.NewServer(endpoints.StatusEndpoint,
				decodeStatusRequest, encodeResponse)
		})
	}

}*/
